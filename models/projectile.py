class Projectile:
    def __init__(self, velocity, x_position, y_position, radius):
        self.velocity = velocity
        self.x_position = x_position
        self.y_position = y_position
        self.radius = radius